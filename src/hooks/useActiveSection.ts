/**
 * Replaces the duplicated scroll-spy IntersectionObserver block
 * from js/index.js, js/experience.js, js/projects.js.
 */
export function useActiveSection(sectionIds: string[]): string | null {
  // TODO: IntersectionObserver over sectionIds, return the id currently in view
  return null;
}

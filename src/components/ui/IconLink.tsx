import type { SocialLink } from "../../types/nav";

interface IconLinkProps {
  link: SocialLink;
}

export function IconLink({ link }: IconLinkProps) {
  // TODO: render the right inline SVG per link.icon (github | linkedin | email)
  return null;
}

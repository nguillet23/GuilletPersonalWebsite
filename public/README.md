# public/

Once the Vite build is wired up (see `updates.md` §8), this directory holds
static assets served as-is: a copy/move of the existing `Content/` folder
(images + `Guillet_Resume.pdf`) and a `favicon.png`, referenced by string
path (e.g. `/Content/Lacrosse.png`) from the data modules in `src/data/`.

Not populated yet — assets still live in `Content/` at the repo root.
